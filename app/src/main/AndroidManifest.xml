<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josh.guesswho">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".TitleActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CharacterSelectionActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".YourTurnActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".OppTurnActivity" />
        <activity android:name=".AskQuestionActivity" />
        <activity android:name=".GuessCharacterActivity"></activity>
    </application>

</manifest>